% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.R
\name{fetch_tax_data}
\alias{fetch_tax_data}
\title{fect and Clean Multiple Sheets from an Excel File}
\usage{
fetch_tax_data(infile)
}
\arguments{
\item{infile}{A string specifying the path to the Excel file.}
}
\value{
A named list of data frames, where each element corresponds to a
sheet from the Excel file.
}
\description{
This \code{fetch_tax_data()} function reads multiple sheets from an Excel file, cleans the data by
removing empty rows, and assigns appropriate column names.
}
\details{
The function performs the following operations on each sheet:
\itemize{
\item Reads the sheet into a \code{data.table} while suppressing warnings.
\item Removes completely empty rows.
\item Identifies the first valid row with meaningful column names.
\item Replaces empty column names with placeholder names (e.g., \code{Column_1}).
\item Assigns the extracted column names and removes unnecessary header rows.
\item Returns a cleaned \code{data.table} for each sheet.
}
}
\examples{
\dontrun{
df_list <- fetch_tax_data("path/to/excel_file.xlsx")
head(df_list[["Sheet1"]])
}

}
