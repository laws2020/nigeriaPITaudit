% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_tax_data.R, R/sum_cols.R
\name{sum_cols}
\alias{sum_cols}
\title{Summarize Selected Numeric Columns in a Data Frame}
\usage{
sum_cols(df, cols_to_sum)

sum_cols(df, cols_to_sum)
}
\arguments{
\item{df}{A data frame containing numeric and non-numeric columns.}

\item{cols_to_sum}{A character vector specifying the names of numeric columns to be summed.}
}
\value{
A modified data frame where the specified numeric columns have an additional "Total" row,
while all other columns remain intact.

A modified data frame where the specified numeric columns have an additional "Total" row,
while all other columns remain intact.
}
\description{
This function computes the column-wise sum of user-specified numeric columns in a given data frame.
It retains all original columns, appending a "Total" row at the bottom for selected columns.
Non-numeric columns remain unchanged, with \code{NA} added to the "Total" row.

This function computes the column-wise sum of user-specified numeric columns in a given data frame.
It retains all original columns, appending a "Total" row at the bottom for selected columns.
Non-numeric columns remain unchanged, with \code{NA} added to the "Total" row.
}
\details{
\itemize{
\item This function supports numeric, factor, and character columns.
\item Character columns in \code{cols_to_sum} are automatically converted to numeric if possible.
\item Non-numeric columns are preserved and filled with \code{NA} in the "Total" row.
\item If \code{cols_to_sum} contains invalid column names, the function returns an error.
}

\itemize{
\item This function supports numeric, factor, and character columns.
\item Character columns in \code{cols_to_sum} are automatically converted to numeric if possible.
\item Non-numeric columns are preserved and filled with \code{NA} in the "Total" row.
\item If \code{cols_to_sum} contains invalid column names, the function returns an error.
}
}
\examples{
df <- data.frame(
  ID = c(1, 2, 3, 4, 5),
  Name = c("Alice", "Bob", "Charlie", "David", "Eve"),
  Age = c(25, 30, 22, 28, 35),
  Income = c("50000", "60000", "45000", "70000", "80000"),
  Status = as.factor(c("Single", "Married", "Single", "Married", "Single")),
  Score = c(85.5, 90.2, 78.0, 88.8, 92.3)
)
sum_cols(df, c("Age", "Income", "Score"))

df <- data.frame(
  ID = c(1, 2, 3, 4, 5),
  Name = c("Alice", "Bob", "Charlie", "David", "Eve"),
  Age = c(25, 30, 22, 28, 35),
  Income = c("50000", "60000", "45000", "70000", "80000"),
  Status = as.factor(c("Single", "Married", "Single", "Married", "Single")),
  Score = c(85.5, 90.2, 78.0, 88.8, 92.3)
)
sum_cols(df, c("Age", "Income", "Score"))

}
